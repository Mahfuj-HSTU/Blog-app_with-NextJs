const AdminDefault = () => {
  return (
    //* if don't want to show the default page return null. can delete the default file. if want to show the default page return the default page, if reload the page then it will show the default page
    null
    // <div>
    //   <h1>
    //     This is admin default page
    //   </h1>
    // </div>
  );
};

export default AdminDefault;